const { gql } = require('apollo-server');

const typeDefs = gql`
  # ------ GraphQL Schema: ------
  # ------ Place your schema below

  # ------ End of schema
`;

module.exports = typeDefs;
